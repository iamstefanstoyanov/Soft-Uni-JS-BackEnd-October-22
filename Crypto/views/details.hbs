        <section id="details-info">
            <h1>Details</h1>
            {{#with crypto}}
            <div class="coin-image">
                <img src="{{imageUrl}}">
            </div>

            <div class="coin-info">
                <div class="coin-text">
                    <h1 id="name">{{name}}</h1>
                    <h3 id="payment">{{paymentMethod}}</h3>
                    <p id="price"><span>{{price}}</p>
                    <p id="description">{{description}}</p>
                </div>
                <div class="product-btn">
                    {{#if isOwner}}
                    <div class="author">
                        <a href="/cryptos/{{_id}}/edit" class="btn-edit">Edit</a>
                        <a href="/cryptos/{{_id}}/delete" class="btn-delete">Delete</a>
                    </div>
                    {{else}}
                         {{#if isGuest}}
                         {{else}}
                            {{#if isBought}}
                            <p class="buy-message">You already bought these crypto coins.</p>
                            {{else}}
                            <a href="/cryptos/{{_id}}/buy" class="btn-buy">Buy</a>
                            {{/if}}
                         {{/if}}
                   {{/if}}
                </div>
            </div>
            {{/with}}
        </section>